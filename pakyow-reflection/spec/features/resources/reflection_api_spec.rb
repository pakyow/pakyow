RSpec.describe "using the reflection api" do
  # TODO: test that can use the reflected api properly, when a reflected scope
  # is set on the connection, and that it fails when nothing is set
  #
  it "needs tests"
end
