RSpec.describe "watching files from the environment" do
  it "calls the callback when the file changes"

  describe "registering an on change callback" do
    it "calls the callback when matched"
  end
end
