RSpec.describe "using the reflect operation" do
  context "from an action" do
    it "can skip verification"
    it "can skip performing"
    it "can skip redirecting"
  end

  context "from an endpoint" do
    it "can skip exposing"
  end
end
